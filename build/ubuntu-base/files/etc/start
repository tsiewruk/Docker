#!/bin/bash

set -e

source_dir="$(dirname "$0")"
source ${source_dir}/../../source/font.sh

SERVICE="${1}"

s6-svc -u "/etc/s6/services/${SERVICE}"
echo-success "Service ${SERVICE} started"
echo "PID"
